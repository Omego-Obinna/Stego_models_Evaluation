opencv-python>=4.5.5
numpy>=1.22.0
pandas>=1.4.0
matplotlib>=3.5.0
seaborn>=0.11.2
scikit-image>=0.18.3
torch>=1.10.0
torchvision>=0.11.1
tqdm>=4.64.0
You can generate it with:
  pip freeze > requirements.txt
But for clean reproducibility, the versions above are conservative and widely compatible.
